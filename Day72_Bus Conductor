#User function Template for python3

class Solution:

    def findMoves(self,n,chairs,passengers):

        chairs.sort()

        passengers.sort()

        ans=0

        for i in range(n):

            x=abs(chairs[i]-passengers[i])

            ans+=x

        return ans

        #code here
